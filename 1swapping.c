//SWAPING PROGRAM USING
/*a-3 var
	int x=10,y=20,temp=0;
		printf("Before : x=%d y=%d\n",x,y);
		temp=x;
		x=y;
		y=temp;
		printf("After :x=%d y=%d",x,y);
		return 0;
b-2 var
	int x=10,y=20;
		printf("Before : x=%d y=%d\n",x,y);
		x=x+y;
		y=x-y;
		x=x-y;
		printf("After :x=%d y=%d",x,y);
		return 0;
c-xor logical

#include<stdio.h>

	int main()
	{
		int x=10,y=20;
		printf("Before : x=%d y=%d\n",x,y);
		x=x^y;
		y=x^y;
		x=x^y;
		printf("After :x=%d y=%d",x,y);
		return 0;	
	}
	
*/















